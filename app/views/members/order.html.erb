

<%= form_for(@order) do |f| %>
  <% if @order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% @order.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<div style="background-color:darkGray; font-size:30;  text-align:center; padding-top:50px;">
	<h1>Order Card</h1>

  <!-- <div class="field">
    <%= f.label :member_id %><br />
    <%= f.text_field :member_id %>
  </div> -->
  <div class="field">
    <%= f.label :quantity %><br />
    <%= f.number_field :quantity, :size=>5 %>
  </div>
  <div class="field">
    <%= f.label :delivery %><br />
    <%= f.text_field :delivery, :size=>5 %>
  </div>

  <!-- <div class="field">
    <%= f.label :status %><br />
    <%= f.text_field :status %>
  </div> -->
  <div class="actions">
    <%= f.submit %>
  </div>

<div>
<% end %>
